{
  "name": "orbital-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A pixel-art offline puzzle game for Chrome.",
  "scripts": {
    "build": "node tools/build.mjs all",
    "build:site": "node tools/build.mjs site",
    "build:extension": "node tools/build.mjs extension",
    "dev": "node tools/serve.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/board.js && node --check src/audio.js && node --check src/navigation.js && node --check background.js && node --check web-sw.js && node --check tools/build.mjs && node --check tools/make-icons.mjs && node --check tools/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
